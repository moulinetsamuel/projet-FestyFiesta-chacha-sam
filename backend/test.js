import {
  Event, User, Object, EventUser,
} from './app/models/index.js';

const run = async () => {
  try {

  } catch (error) {
    console.error('Erreur lors de la création', error);
  }
};

run();
